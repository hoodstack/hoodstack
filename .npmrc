strict-peer-dependencies=false
auto-install-peers=true
# Keep transitive dependencies out of the root of node_modules so packages cannot
# accidentally import something they did not declare. Guards against phantom deps.
node-linker=isolated
